pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'npm i'
            }
        }
    stage('Test') {
            steps {
                sh 'npx cypress open'
            }
        }
    stage('Deploy') {
            steps {
                echo 'Success deploying...'
            }
        }
    }
}
